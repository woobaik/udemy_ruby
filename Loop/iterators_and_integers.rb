# Ranges
(3..7).each {|n| puts n}
(-5..7).each {|n| puts n}

# do some x times
x = 9
(1..9).each {puts "hello"}
(0...9).each {puts "hi"}

# We can actually skip the step where we create a range
# x.upto(y) iterate over range x..y
# x.times -> not worry about variable
